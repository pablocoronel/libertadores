extends layouts/main

block content
    .container
        .row
            .col-12.d-flex.justify-content-center
                h1 #{story.title}

        .row
            .col-12
                .container!= story.content